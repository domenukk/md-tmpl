# Benchmark dependencies for md-tmpl vs Jinja2/Mako/Chevron/Django.
# md-tmpl is installed from the local PyO3 package
# (see benchmarks/README.md for setup instructions).
jinja2>=3.1
Mako>=1.3
chevron>=0.14
Django>=5.0
