{
  "name": "md-tmpl-wasm-tests",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && tsc -p benchmarks",
    "test": "node dist/correctness.js && node --test dist/wasm.test.js",
    "bench": "node dist/bench.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
